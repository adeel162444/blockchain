import { supabase } from '../config/supabase.js';

export const createAttendanceRecord = async (attendanceData) => {
};

export const findAttendanceByClass = async (classId) => {
};

export const findAttendanceByStudent = async (studentId) => {
};

export const findAttendanceByDate = async (date) => {
};
